source 'https://api.berkshelf.com'

metadata

cookbook 'monit', github: 'phlipper/chef-monit', tag: '1.5.2'
cookbook 'monit_wrapper', '~> 2.4'

group :integration do
  cookbook 'minitest-handler'
end
